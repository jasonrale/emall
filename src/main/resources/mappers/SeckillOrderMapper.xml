<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.emall.dao.SeckillOrderMapper">
    <resultMap id="BaseResultMap" type="com.emall.entity.SeckillOrder">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="so_id" jdbcType="VARCHAR" property="soId"/>
        <result column="u_id" jdbcType="INTEGER" property="uId"/>
        <result column="g_id" jdbcType="INTEGER" property="gId"/>
        <result column="o_id" jdbcType="INTEGER" property="oId"/>
    </resultMap>

    <sql id="Base_Column_List">
        so_id, u_id, g_id, o_id
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from seckill_order
        where so_id = #{soId,jdbcType=VARCHAR}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.String">
        delete
        from seckill_order
        where so_id = #{soId,jdbcType=VARCHAR}
    </delete>

    <insert id="insert" parameterType="com.emall.entity.SeckillOrder">
        insert into seckill_order (so_id, u_id, g_id, o_id)
        values (#{soId,jdbcType=VARCHAR}, #{uId,jdbcType=INTEGER}, #{gId,jdbcType=INTEGER},
                #{oId,jdbcType=INTEGER})
    </insert>

    <insert id="insertSelective" parameterType="com.emall.entity.SeckillOrder">
        insert into seckill_order
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="soId != null">
                so_id,
            </if>
            <if test="uId != null">
                u_id,
            </if>
            <if test="gId != null">
                g_id,
            </if>
            <if test="oId != null">
                o_id,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="soId != null">
                #{soId,jdbcType=VARCHAR},
            </if>
            <if test="uId != null">
                #{uId,jdbcType=INTEGER},
            </if>
            <if test="gId != null">
                #{gId,jdbcType=INTEGER},
            </if>
            <if test="oId != null">
                #{oId,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.emall.entity.SeckillOrder">
        update seckill_order
        <set>
            <if test="uId != null">
                u_id = #{uId,jdbcType=INTEGER},
            </if>
            <if test="gId != null">
                g_id = #{gId,jdbcType=INTEGER},
            </if>
            <if test="oId != null">
                o_id = #{oId,jdbcType=INTEGER},
            </if>
        </set>
        where so_id = #{soId,jdbcType=VARCHAR}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.emall.entity.SeckillOrder">
        update seckill_order
        set u_id = #{uId,jdbcType=INTEGER},
            g_id = #{gId,jdbcType=INTEGER},
            o_id = #{oId,jdbcType=INTEGER}
        where so_id = #{soId,jdbcType=VARCHAR}
    </update>
</mapper>